---
en:
  gateways:
    edit:
      page_title: Edit gateway settings
    form:
      additional_info_about_the_merchant: Additional info about the merchant
      all_fields_are_optional: "(all fields are optional)"
      currency_conversion_options: Currency conversion options
      electrum_wallet: your Electrum
      hint: |
        If one exchange rate provider fails due to being unreachable, the other one will be used automatically. Remember, this only applies if the gateway's default currency is different from BTC.
      more_info: Watch a short video and learn how to export BIP32 pubkey from %{electrum_url} or %{mycelium_wallet_url}.
      mycelium_wallet: Mycelium wallet
      rate_provider_1: 'Exchange rate provider #1:'
      rate_provider_2: 'Exchange rate provider #2:'
      rate_provider_3: 'Exchange rate provider #3:'
      send_notifications_about_new_payments_to_my_email: Send notifications about new payments to my email
      testnet_settings: TESTNET settings
      tip: If you wish to convert all payments into fiat money, you need to <a href='%{path}'>configure an exchange</a> first.
    index:
      active: ACTIVE?
      bip32_testpubkey: BIP32 (TEST)PUBKEY
      create_gateway: create gateway
      create_widget: create widget
      default_currency: DEFAULT CURRENCY
      gateways: gateways
      name: NAME
      no_records:
        statement:
          gateway: |
            A gateway is a logical unit that serves one online store or website. For each store that you own, you need to create one gateway. You can then track all the orders associated with that gateway, along with their statuses and how much money was received.
          widget: |
            A widget is a small block that you can install onto your website that will allow your users to instantly pay you with Bitcoin. It is the easiest way to accept payments with Gear.
        title: You have not created any %{objects} yet
      owner: OWNER
      page_title_if_admin: Your %{objects}
      page_title_unless_admin: All %{objects}
      test_mode: TEST MODE?
      widgets: widgets
    new:
      page_title: Create a new gateway
    show:
      address_derivation_scheme: Address derivation scheme
      addresses: Addresses
      api_gateway_id: API Gateway ID
      bip32_pubkey: BIP32 pubkey
      callback_url: Callback URL
      city: City
      confirmations_required: Confirmations required
      country: Country
      default_currency: Default currency
      edit_settings: Edit settings
      exchange: Exchange
      exchange_rate_providers: Exchange rate providers
      form:
        hint: |
          This is for you to make sure that addresses that are going to be used to generate orders are the same as in your wallet. Make sure they match. You can adjust the consecutive index above, so you are able to check more addresses generated by your wallet.
      in_test_mode: In test mode?
      is_active: Is active?
      locale: Gateway Locale
      online_store_site_url: Online store / site URL
      orders_expiration_period: Orders expiration period
      orders_stat: Orders stat
      page_title: "%{name} | Gateways"
      region: Region
      seconds: seconds
      secret: 'Your gateway secret is: <b>%{message}</b>'
      see_all_link: See all orders for this gateway
      warning: |
        Gateway settings saved. Please write this down. It is only displayed here once. This key is needed to access the API when creating orders and to sign callbacks. If you later lose this key, you will need to regenerate it.
