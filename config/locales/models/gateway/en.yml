---
en:
  gateway:
    errors:
      adapter:
        unavailable: "includes adapter %{a}Adapter which is not available"
      pubkey:
        invalid: "doesn't look like a BIP32 pubkey"
      address_derivation_scheme:
        invalid: "doesn't look like address derivation scheme"
      currency:
        unsupportable: "%{name} doesn't support this currency"
      test_mode:
        blank_pubkey: "can't be blank if test mode is activated"