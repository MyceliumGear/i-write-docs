---
- name: Copy dotenv files
  template: src=dotenv.j2
            dest={{ item.shared_dir }}/.env.{{ item.env }}
            owner={{ deploy_user }} group={{ deploy_user }} mode=440
  with_items: provision_apps
  tags: dotenv
